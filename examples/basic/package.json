{
  "name": "fanlight-example-basic",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "webpack",
    "clean": "rm -rf node_modules && rm -rf dist",
    "reserver": "npm update && npm run build && npm run server",
    "server": "webpack-dev-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nicholas Frees",
  "license": "ISC",
  "dependencies": {
    "fanlight": "github:xanister/fanlight"
  },
  "devDependencies": {
    "webpack": "^4.12.0",
    "webpack-cli": "^3.0.8",
    "webpack-dev-server": "^3.1.4"
  }
}
